pipeline {
  agent {
        docker {
            image 'maven:latest'
        }
    }
  stages {
    stage('check version') {
      steps {
        sh '''
          mvn --version
          git --version
        '''
      }
    }
  }
}